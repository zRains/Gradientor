export interface APP {
    gridNum: number
    colorArr: string[]
    searchText: string
    compareColor: string
    useMode: boolean
    popMsg: string
}
export interface emitColor {
    color: string
    originalColor: string
}

